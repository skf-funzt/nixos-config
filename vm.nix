{
  virtualisation.vmVariant = {
    # following configuration is added only when building VM with build-vm
    # virtualisation = {
    #   memorySize =  4096;
    #   cores = 4;
    #   diskSize = 4096;
    # };
    virtualisation = {
      memorySize =  16384;
      cores = 8;
      diskSize = 4096;
    };
  };
}