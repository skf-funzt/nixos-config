# home/desktop-gnome.nix
# GNOME-specific home settings.
{ config, pkgs, lib, ... }:

{
  # GNOME desktop home settings go here.
  # Currently minimal — GNOME is configured at system level in modules/desktop/gnome.nix.
}
